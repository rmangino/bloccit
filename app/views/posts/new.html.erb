<h1>New Post</h1>
<div class="row">
  <div class="col-md-4">
    <p>Guidelines for posts</p>
    <ul>
      <li>Make sure it rhymes.</li>
      <li>Don't use the letter "A".</li>
      <li>Don't overdo hashtags.</li>
    </ul>
  </div>

  <div class="col-md-8">
    <%= form_for [@topic, @post], builder: ApplicationHelper::LabellingFormBuilder do |f| %>
      <div class="form-group">
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter post title" %>
      </div>

      <div class="form-group">
        <%= f.label "Body" %>
        <%= f.text_area :body, class: 'form-control', placeholder: "Enter post body" %>
      </div>

      <%= f.fields_for :summary do |summary_form| %>
        <div class="form-group">
          <%= summary_form.label "Summary" %>
          <%= summary_form.text_area :description, class: 'form-control', placeholder: "Enter post summary" %>
        </div>
      <% end %>

      <div class="form-group">  
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>